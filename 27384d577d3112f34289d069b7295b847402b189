Correction: the phrase 'generated with grok's own mcp-add command' lost two words in
the original message. A backtick pair in the commit text was expanded by the shell before
git saw it, so the command name was substituted away.

The intended sentence: MCP configuration is TOML at .grok/config.toml, and it should be
generated by running grok's own 'mcp add' subcommand rather than hand-written, because grok
owns that file's shape.

Not amended: this commit is an ancestor of the v0.5.0 tag and its published release, so
rewriting it would leave the release pointing at an object that no longer exists. A note
corrects the record without breaking what has already shipped.
